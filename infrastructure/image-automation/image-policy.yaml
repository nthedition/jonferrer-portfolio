---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: portfolio-site
  namespace: flux-system
spec:
  filterTags:
    extract: $num
    pattern: ^(?P<num>[0-9]+)-[a-f0-9]+$
  imageRepositoryRef:
    name: portfolio-site
  policy:
    numerical:
      order: asc
